{.passL:"-lvapoursynth".}

import lib/[consts, libapi, libcore, helper, libvsplugins, libvsmap, output, info, libvsnode] # libvsanode,
export consts, libapi, libcore, helper, libvsplugins, libvsmap, libvsnode, output, info

import plugins4/all_plugins
export all_plugins

import lib/audio/[audioformat, audioinfo]
export audioformat, audioinfo